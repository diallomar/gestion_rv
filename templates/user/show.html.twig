{% extends 'base.html.twig' %}

{% block title %}utilisateur
{% endblock %}

{% block body %}
	<h1>utilisateur</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ user.id }}</td>
			</tr>
			<tr>
				<th>Email</th>
				<td>{{ user.email }}</td>
			</tr>
			<tr>
				<th>Roles</th>
				<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
			</tr>
			<tr>
				<th>Password</th>
				<td>{{ user.password }}</td>
			</tr>
		</tbody>
	</table>
	<div class="row">
		<div class="col-lg-3 col-md-6 col-12">
			<a href="{{ path('user_index') }}" class="btn btn-outline-success">retour</a>
		</div>
		{# <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a> #}
		<div class="col-lg-3 col-md-6 col-12">
			{{ include('user/_delete_form.html.twig') }}
		</div>
	{% endblock %}
